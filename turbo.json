{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".next/**"]
    },
    "lint": {},
    "typecheck": {},
    "test": {
      "dependsOn": ["lint", "typecheck"]
    },
    "dev": {
      "cache": false
    }
  }
}
