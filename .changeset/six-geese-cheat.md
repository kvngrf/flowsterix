---
'@flowsterix/react': patch
---

Refresh the DevTools UI to align with Flowsterix landing brand colors and introduce a new collapsed bubble state.

When collapsed, DevTools now render as a round floating bubble that expands back into the full panel with a clip-path animation.
